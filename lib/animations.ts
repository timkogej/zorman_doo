// Shared animation config — use directly in motion components
export const EASE = [0.25, 0.1, 0.25, 1] as const;

export const VIEWPORT = { once: true, margin: "-80px" } as const;

export const STAGGER_DELAY = 0.12;
